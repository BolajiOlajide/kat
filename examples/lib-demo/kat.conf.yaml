# This file configures your database connection and migration settings

migration:
  tablename: migrations
  directory: migrations

verbose: true
database:
  driver: postgres
  url: postgres://bolaji:andela@localhost:5432/kat-demo
  # driver: sqlite
  # path: ./kat.db
