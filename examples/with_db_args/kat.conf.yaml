# yaml-language-server: $schema=../../schemas/kat.conf.schema.json
migration:
  tablename: migrations
  directory: migrations

database:
  user: bolaji
  password: [REDACTED:password]
  name: kat
  port: "5432"
  sslmode: disable
  host: localhost
